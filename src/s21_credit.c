#include "s21_calc.h"
#define DIFF 1
#define ANN 2

bool MainCredit(int total_sum, int timeframe, double rate, int type) {
    bool status = true;

    if (type == DIFF) {
        
    } else {

    }
}